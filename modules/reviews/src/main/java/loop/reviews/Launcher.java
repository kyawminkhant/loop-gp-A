package loop.reviews;

/**
 * Plain launcher (does NOT extend Application) so the app can be started
 * as a normal Java Application from an IDE without the
 * "JavaFX runtime components are missing" error. Delegates to App.main.
 */
public class Launcher {
    public static void main(String[] args) {
        App.main(args);
    }
}
